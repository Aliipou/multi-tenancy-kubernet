{{- if and .Values.dashboardService.enabled .Values.dashboardService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "saas-app.fullname" . }}-dashboard-hpa
  namespace: {{ .Values.tenant.namespace }}
  labels:
    {{- include "saas-app.dashboardService.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "saas-app.fullname" . }}-dashboard
  minReplicas: {{ .Values.dashboardService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dashboardService.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.dashboardService.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.dashboardService.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.dashboardService.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.dashboardService.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
      - type: Pods
        value: 2
        periodSeconds: 60
      selectPolicy: Min
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30
      - type: Pods
        value: 4
        periodSeconds: 30
      selectPolicy: Max
{{- end }}
